1.   A> In JavaScript, -Infinity(negative infinity) is a special numeric value that is smaller than any other    representable number.
        
      B> In JavaScript, Infinity is a numeric value representing positive infinity that is greater than any other maximum representable number. It is a read-only property of the global object and behaves as a standard numeric primitive type when evaluated.

2. Secondlargest number program  will check duplicate, also if array is empty or 1 element.